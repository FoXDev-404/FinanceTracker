/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    // Set NEXT_PUBLIC_API_URL in your Vercel environment variables
    // to point to your Railway backend, e.g. https://your-app.up.railway.app/api
  },
};

export default nextConfig;
